import { LoginForm } from '@/components/professionals/LoginForm'

export const metadata = {
  title: 'Login | Partners Area | Xpert Connect',
}

export default function PartnerLoginPage() {
  return <LoginForm />
}
