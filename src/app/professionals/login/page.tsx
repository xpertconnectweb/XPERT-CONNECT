import { LoginForm } from '@/components/professionals/LoginForm'

export const metadata = {
  title: 'Login | Professionals Area | Xpert Connect',
}

export default function LoginPage() {
  return <LoginForm />
}
